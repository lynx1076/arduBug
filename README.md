# arduBug
A debugger, logic analyzer and EEPROM programmer for the n65 modular computer

### Compiling
Currently the application has only been tested and confirmed to work with the compilers set in the respective Makefiles.
Unexpected behaviour may occurr when using a different compiler to build the host software or firmware.

